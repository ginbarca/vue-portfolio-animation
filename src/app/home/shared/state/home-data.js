/** Initial state */
const initialState = {
    loading: false,
    data: null,
    error: null
};

/**
 * Users data mutations
 */
const mutations = {};

/**
 * Users data actions
 */
const actions = {};

export default {
    mutations,
    actions,
    state: initialState
};
